# Cron Job Configuration
# Enable or disable the cron job (true/false)
CRON_ENABLED=true

# Cron schedule expression (standard crontab format)
# Examples:
#   "0 0 * * *"     - Run daily at midnight
#   "0 */6 * * *"   - Run every 6 hours
#   "0 9 * * 1"     - Run every Monday at 9 AM
#   "0 0 1 * *"     - Run on the 1st day of every month at midnight
CRON_SCHEDULE=0 0 * * *

# Timezone for cron job execution (e.g., America/New_York, UTC, Europe/London)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
CRON_TIMEZONE=America/Chicago

# Comma-separated list of Letterboxd usernames to fetch movies for
# Example: user1,user2,user3
CRON_TARGET_USERS=

# TMDb (The Movie Database) API Configuration
# Get your API key from: https://www.themoviedb.org/settings/api
TMDB_API_KEY=

# TMDb v4 Access Token (for read operations only)
# Get your v4 access token from: https://www.themoviedb.org/settings/api
TMDB_V4_ACCESS_TOKEN=

# TMDb Account Authentication (REQUIRED for list creation/management)
# Option 1: Provide your TMDb username and password
TMDB_USERNAME=
TMDB_PASSWORD=

# Option 2: Or provide a pre-existing session_id (if you have one)
# TMDB_SESSION_ID=

# Enable/disable TMDb sync (true/false)
TMDB_SYNC_ENABLED=false

# Note: Per-User List Architecture
# ================================
# Each Letterboxd user automatically gets their own TMDb list.
# List names are auto-generated as: "{username}'s Top Rated Movies"
# List IDs are automatically tracked in tmdb_list_mappings.json
# No manual list configuration needed!
